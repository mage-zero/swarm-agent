{
  "version": "0.4.55",
  "date": "2026-02-23",
  "summary": "Fix deploy failure caused by ddtrace stderr noise in app:config:status",
  "requires": {
    "cloud-swarm": ">=1.0.18"
  },
  "changes": [
    {
      "id": "config-status-prefer-stdout",
      "description": "app:config:status now reads stdout first for status parsing. Previously it preferred stderr, which could contain unrelated noise from PHP extensions like ddtrace, causing the deploy to fail with a spurious config mismatch error.",
      "phase": "pre_migrate",
      "downtimeMinutes": 0,
      "scope": "stack"
    }
  ]
}

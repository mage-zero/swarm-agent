{
  "version": "0.4.57",
  "date": "2026-02-23",
  "summary": "Fix maintenance mode on multi-node clusters where redis-cache is on a different node",
  "requires": {
    "cloud-swarm": ">=1.0.22"
  },
  "changes": [
    {
      "id": "redis-maintenance-via-admin",
      "description": "setMaintenanceModeViaRedis now executes redis commands via the php-fpm-admin container over the overlay network instead of requiring a local redis-cache container. Fixes deploy failures on multi-node clusters when Swarm schedules redis-cache on a worker node.",
      "downtimeMinutes": 0,
      "scope": "deploy"
    }
  ]
}

{
  "version": "0.4.54",
  "date": "2026-02-23",
  "summary": "Scale down cron service during setup:upgrade to prevent OOM kills",
  "requires": {
    "cloud-swarm": ">=1.0.18"
  },
  "changes": [
    {
      "id": "scale-cron-during-upgrade",
      "description": "Scale the cron service to 0 replicas before setup:upgrade and restore it afterwards. The cron container runs php bin/magento cron:run in a loop which loads the full Magento DI container (~135MB+ per invocation) even in maintenance mode, causing OOM kills on memory-constrained nodes.",
      "phase": "deploy",
      "downtimeMinutes": 0,
      "scope": "stack"
    }
  ]
}
